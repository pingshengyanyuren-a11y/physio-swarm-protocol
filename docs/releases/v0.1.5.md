# v0.1.5

## Added

- `AdaptiveLatentModel` for online learned latent representations
- continuous field `tick()` and `field_strength()` behavior in `SemanticVectorBus`
- `DistributedTissueExecutor` for process-based tissue execution

## Changed

- runtime now trains latent representations from task experience
- vector signaling no longer depends only on deterministic embeddings
- tissue execution can run in parallel worker processes while preserving region context

## Verified

- `python -m unittest discover -s .\tests -v`
- `python .\examples\research_assistant_demo.py`
- `python -m pip install -e .`
