# v0.1.4

## Added

- `TissueTopology` for local tissue neighborhoods and region placement
- regional `CirculatorySystem` state inside the runtime
- local-resonance latent propagation in `SemanticVectorBus`
- immune hazard memory recall in `MemoryGraph`

## Changed

- runtime routing now prefers local tissue cells before distant cells
- tasks carry `region` and `propagation_hops`
- cells now carry `region`
- config-based cell construction can set `region`

## Verified

- `python -m unittest discover -s .\tests -v`
- `python .\examples\research_assistant_demo.py`
- `python .\scripts\run_workflow.py .\examples\workflows\research-assistant.toml`
