# v0.1.3

## Added

- OpenAI-compatible `RemoteLLMAdapter` for real networked provider execution
- `MemoryGraph` with durable interaction recall and trust-score curriculum
- `SemanticVectorBus` for latent vector communication and similarity-based recall
- `PersistentScheduler` with SQLite-backed task claiming and completion tracking

## Changed

- Runtime now updates memory and trust after each execution
- Nervous routing can consider trust scores when selecting cells
- Signal history now includes latent vector traffic in addition to endocrine and immune signals

## Verified

- `python -m unittest discover -s .\tests -v`
- `python .\scripts\run_workflow.py .\examples\workflows\research-assistant.toml`
- `python -m pip install -e .`
